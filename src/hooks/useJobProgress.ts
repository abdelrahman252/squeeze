// Placeholder — wired up in Phase 6 with Tauri Channel<ProgressEvent>
// This hook will subscribe to progress events from the Rust backend
// and update the jobs store via useJobsStore.getState().updateJob(...)

export {};
